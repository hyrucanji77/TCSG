# Reproducibility & Audit Trail (CC-BY-4.0)
- Record: (i) SHA256 of `config.json`, (ii) git commit, (iii) Docker digest, (iv) SHA256 of inputs/outputs.
- Provide one-command `scripts/make_audit.sh` to reproduce metrics and report.
